# STAGING ENVIRONMENT - backend.conf
# Usage: terraform init -backend-config=backend.conf

bucket         = "myapp-terraform-state-staging"
key            = "staging/terraform.tfstate"
region         = "us-east-1"
dynamodb_table = "myapp-terraform-state-lock"
encrypt        = true
